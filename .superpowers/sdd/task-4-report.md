# Task 4 — отчёт о выполнении

Дата: 2026-09-19  
Ветка: `feat/evidence-harness-v2`

## Результат

Добавлен явно загружаемый `AnalyticsCatalog` с 12 разрешёнными отношениями,
ручным списком полей, связями с direction/cardinality и отдельными определениями
персональных поручений, завершённых заданий и KPI просрочки. `catalog.json`
копируется рядом с приложением как `catalog.json`.

Добавлен `EmployeeResolver`: поиск использует до пяти параметризованных токенов,
экранирует wildcard-символы `ILIKE`, требует совпадения всех токенов в имени и
считает сотрудниками только получателей, встречающихся как performer задания.
Переполнение 20 кандидатов возвращается как `too_many_candidates`; закрытый
статус не скрывается и включается в описание кандидата.

## TDD-свидетельства

1. RED: запуск `catalog` завершился CS0246 — отсутствовал `AnalyticsCatalog`.
2. GREEN: `catalog` — **PASS 6 / FAIL 0**.
3. В DB suite добавлены сценарии двух однофамильцев, подразделения с похожим
   именем, отдельного сотрудника, отсутствующего ID, wildcard и 21 кандидата.
   Все имена и ID в `db/init.sql` вымышлены; данные стенда не использовались.

## Проверки

- `all` (без DB): **PASS 40 / FAIL 0**;
- `catalog`: **PASS 6 / FAIL 0**;
- `contracts`: **PASS 14 / FAIL 0**;
- `results`: **PASS 9 / FAIL 0**;
- `sql`: **PASS 11 / FAIL 0**;
- `dotnet build -c Release`: **PASS**, 0 warnings / 0 errors;
- IDE diagnostics: ошибок нет;
- `db`: **BLOCKED** — Docker daemon недоступен, подключение к RX не выполнялось.

## Ограничения

- Каталог первой поставки является ручным версионируемым allowlist; неизвестная
  таблица или колонка возвращает `schema_mismatch`, без попытки угадать поле.
- Интеграционные тесты резолвера подготовлены, но требуют пересоздания
  изолированного PostgreSQL-контейнера после запуска Docker Desktop.
