# build output (пересобирается)
bin/
obj/
.vs/
*.user
# портативный self-contained бандл (~79 МБ) — артефакт, пересобирается: dotnet publish -r win-x64 --self-contained
dist/
# конфиг с секретами (пароль БД, токен LLM) — НЕ коммитить; задаётся через Бэк-офис
config.json
# лог фонового запуска сервера при разработке
server.log
# сторонние материалы, не наш код — не затягивать в репозиторий
sungero-remote-component-example-react-main/
2026-07-22-mvp-screen-final.html
# дубль мокапа в корне — оригинал лежит в Design Reference/
/Мокап рабочего стола RX.html
